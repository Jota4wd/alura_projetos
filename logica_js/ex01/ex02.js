// Verifique se um número digitado pelo usuário é positivo ou negativo.
// Mostre um alerta informando.

let nbr = Number(prompt('numero: '));

if (nbr >= 0){
    alert('positivo');
} else {
    alert('negativo');
}
